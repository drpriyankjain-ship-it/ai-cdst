# Python Pipeline — Archived

This directory contains the original Python implementation of the CDST backend pipeline.

**This code is not in active use.** The live pipeline is the Node.js implementation in `server/`.

Do not make changes here. All pipeline changes should be made in `server/stages/`.
